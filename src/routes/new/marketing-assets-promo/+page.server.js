/** @type {import('./$types').PageServerLoad} */
export async function load({ params }) {
	return {
		title: 'Get marketing assets to promote your Momentum page',
		ogTitle: 'Get marketing assets to promote your Momentum page'
	};
}
